apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cortex-os.fullname" . }}-test-connection"
  labels:
    {{- include "cortex-os.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cortex-os.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
